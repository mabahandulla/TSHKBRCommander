package enumsStore;

/**
 * The enum class for storing switches to select the required circuit.
 * <p>
 * @author Aleksander Demichev
 * @version 1.0.0.0
 */

public enum ModeSwitches {

	GET_PROM,
	GET_TEST,
	SET_PROM,
	SET_TEST;
}
